name: logs
description: "Small, feature-rich logging package for Dart and Flutter. Written in pure Dart, with no dependencies."
version: 0.0.1
homepage: https://github.com/dev-kasibhatla/logs
repository: https://github.com/dev-kasibhatla/logs
issue_tracker: https://github.com/dev-kasibhatla/logs/issues

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: